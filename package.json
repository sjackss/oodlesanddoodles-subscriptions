{
  "name": "oodlesanddoodles-subscriptions",
  "version": "1.0.0",
  "description": "Paddle subscription system for Oodles & Doodles",
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'Build complete'",
    "deploy": "netlify deploy --prod"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^4.6.0"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  }
}
